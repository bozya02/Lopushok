<Window x:Class="Lopushok.Windows.MaterialCountWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lopushok.Windows"
        mc:Ignorable="d" Height="150" Width="200"
        ResizeMode="NoResize">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Text="Введите количество"/>
        <TextBox x:Name="tbCount" Grid.Row="1" Text="{Binding MaterialQuantity}"
                 PreviewTextInput="tbCount_PreviewTextInput"/>
        <Button x:Name="btnSave" Grid.Row="2" Click="btnSave_Click" Content="Ок"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"/>
    </Grid>
</Window>
